kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: roboshop-efs
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: fs-027a977db566a8ac4 # we have to give our filesystem id
  directoryPerms: "700"
  basePath: "/roboshop" # optional
